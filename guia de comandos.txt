git init (criar repositório local)

git remote add origin ´link do github`

git add . 

git commit -m `mensagem do commmit`

git push origin main

*agora estamos em outra branch

git checkout -b ´nome da nova brench´